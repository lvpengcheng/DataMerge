# DataMerge 产品功能与设计文档

> 文档类型：产品功能说明 + 使用说明 + 系统设计说明  
> 适用系统：DataMerge（AI 驱动的 Excel 数据整合与自动化处理平台）  
> 当前实现基线：2026-09-14 代码库  
> 主要业务场景：薪资、考勤、社保、个税及其他多 Excel 数据加工场景

## 1. 文档目的

本文档汇总 DataMerge 当前已经实现的主要功能，说明每项功能的用途、适用场景、使用方法、输入输出、权限边界和关键规则，同时描述系统的核心流程、数据模型、接口分区和运行机制。

本文档可用于：

- 产品演示、需求评审和用户培训；
- 业务人员日常操作参考；
- 开发、测试、实施和运维人员理解系统边界；
- 新租户上线、脚本验收和问题排查。

## 2. 产品概述

DataMerge 是一套面向企业 Excel 数据处理场景的多租户平台。系统把人工维护的业务规则、源 Excel、目标结果样例和 AI 代码生成结合起来，形成“规则整理—智能训练—脚本验证—正式计算—报表输出—历史追溯”的闭环。

系统的核心价值包括：

1. 将散落在文档、表格和人员经验中的规则整理为结构化规则；
2. 根据源数据、目标模板和业务规则自动生成 Python 处理脚本；
3. 通过实际运行结果与预期结果逐单元格对比，持续修正脚本；
4. 将验证后的脚本版本化保存，供后续月份重复计算；
5. 提供拆表、合表、整合对比、智能组表、SOP 审核等通用工具；
6. 通过用户、角色、组织和租户授权实现企业级数据隔离和权限控制。

## 3. 用户角色与典型分工

系统不把业务角色写死，而是由管理员通过“角色权限”配置实际能力。常见分工如下：

| 角色 | 典型职责 |
| --- | --- |
| 业务操作员 | 整理规则、发起智训、运行智算、下载结果、使用智能小工具 |
| 业务复核员 | 检查训练差异、选择最佳脚本、审核 SOP、确认智能组表映射 |
| 实施/开发人员 | 上传或修正代码、维护模板、排查执行日志、迁移脚本 |
| 组织管理员 | 管理本组织业务方案、租户授权、基础数据和可见范围 |
| 系统管理员 | 管理用户、角色、组织、模板、脚本、AI 模型和全局规则 |

管理员角色拥有全部权限。普通用户只看到角色授权的菜单和操作，并受组织树及租户授权范围限制。

## 4. 功能地图

| 一级入口 | 主要功能 | 主要产物 |
| --- | --- | --- |
| 登录 | 登录、退出、查看当前用户、修改密码 | 登录令牌、用户权限上下文 |
| 首页 | 租户本月计算状态总览、待办跳转 | 租户计算状态卡片 |
| 规则 | AI 对话式规则整理、会话管理、规则下载 | `rules.md` |
| 智训 | 文件解析、AI 生成代码、执行验证、差异迭代、人工选优 | Python 脚本、试算结果、差异报告、最终规则 |
| 智算 | 选择脚本版本、上传当期数据、正式计算、下载结果 | 计算结果 Excel、日志、历史任务 |
| 智能小工具 | Sheet 拆分、多表合并、多表整合对比、模板、历史、数据对比、SOP、智能组表 | Excel、ZIP、PDF、差异报告、方案、SOP 记录 |
| 管理 | 用户、角色、组织、租户、基础数据、脚本、迁移、规则和模型管理 | 系统配置及治理数据 |

## 5. 核心业务闭环

### 5.1 标准流程

1. 在“规则”中上传源文件、目标文件和设计文档，通过 AI 对话整理业务规则；
2. 下载整理好的 `rules.md`，必要时由业务人员补充或确认；
3. 在“智训”中选择租户，上传源文件、目标文件和规则文件；
4. 选择 AI 模型、生成模式及薪资年月等配置，开始训练；
5. 系统解析 Excel 结构，生成脚本并在隔离环境中执行；
6. 系统将试算结果与预期结果比较，展示准确率和差异明细；
7. 用户通过对话讨论原因，选择“执行修正”继续迭代，或上传人工代码验证；
8. 验证通过后将当前代码“设为最佳”，形成可复用脚本版本；
9. 在“智算”中选择租户和脚本版本，上传当月源文件并执行；
10. 下载结果，必要时在“计算历史”中用报表模板生成正式交付文件。

### 5.2 训练与计算的关系

- 智训解决“规则如何变成可执行程序”的问题；
- 智算解决“已验证程序如何重复处理新一期数据”的问题；
- 每个脚本归属于一个租户，并保存来源会话、模式、版本、训练准确率、源结构、目标结构和手动表头配置；
- 智算默认只使用启用状态的脚本，用户可以选择具体版本；
- 训练样例发生结构性变化时，应重新训练或重新验证，不应只依赖历史准确率。

## 6. 登录与账号功能

### 6.1 功能介绍

系统采用用户名和密码登录。登录成功后，前端保存访问令牌，并根据当前用户的角色权限控制菜单、按钮和数据范围。

### 6.2 使用说明

1. 打开登录页；
2. 输入用户名和密码；
3. 点击“登录”；
4. 登录成功后进入系统首页；
5. 在用户信息区域可退出登录；
6. 用户可通过修改密码接口更新自己的密码，管理员也可为用户重置或设置密码。

### 6.3 关键规则

- 未登录、令牌无效或令牌过期时，受保护接口返回未认证；
- 已停用用户不能继续使用系统；
- 后端会再次校验权限，前端隐藏按钮不是唯一安全措施；
- 密码以哈希形式保存，不以明文保存用户登录密码。

## 7. 首页：租户计算总览

### 7.1 功能介绍

首页按租户展示当月是否已经完成计算，帮助业务人员快速识别待处理租户。

### 7.2 展示内容

- 当前用户可访问的租户总数；
- 本月已计算数量和待计算数量；
- 每个租户的“本月已计算/本月未计算”状态；
- 上月最后计算时间和耗时；
- 对未计算租户提供“去计算”快捷入口。

### 7.3 使用说明

进入首页后系统自动加载数据。待计算租户优先显示；点击“去计算”会进入智算页并预填对应租户。

## 8. 规则整理

### 8.1 功能介绍

规则整理用于把源表结构、目标表结构、设计说明和用户补充要求整理成结构清晰、可供智训使用的 `rules.md`。该模块采用会话式交互，支持持续追问、补充和修订。

### 8.2 支持输入

| 输入 | 是否必需 | 支持格式 | 说明 |
| --- | --- | --- | --- |
| 源文件 | 首次整理必需 | `.xlsx`、`.xls` | 可上传多个，供 AI 理解源表结构 |
| 目标文件 | 可选 | `.xlsx`、`.xls` | 用于说明期望输出结构 |
| 设计文档 | 可选 | `.pdf`、`.docx`、`.doc`、`.txt`、`.md`、`.xlsx`、`.xls` | 可包含字段映射、口径、计算步骤和例外规则 |
| 整理要求 | 建议填写 | 文本 | 对 AI 的补充说明或追问 |
| AI 模型 | 必选 | 管理员启用的模型 | 当前支持 DeepSeek、Claude、OpenAI 等提供者 |

### 8.3 使用说明

1. 点击“新建”建立空白会话；
2. 点击附件按钮，至少选择一个源文件；
3. 可选上传目标文件和设计文档；
4. 选择 AI 模型；
5. 输入整理要求并点击“发送”；
6. 查看 AI 输出，继续追问或要求调整；
7. 内容确认后点击“下载 rules.md”；
8. 历史会话可重新打开或删除，“清空”会开始新会话。

### 8.4 关键能力与规则

- 自动读取 Excel 表头和结构信息；
- 支持流式显示 AI 输出和思考进度；
- 会话保存源文件名、目标文件名、设计文档名、消息历史和最终结果；
- 支持检测受密码保护的 Excel，并提示输入相应密码；
- 管理员可统一控制下拉框中可用的 AI 模型；
- 输出规则应至少覆盖文件角色、字段映射、计算口径、数据清洗、异常处理、输出结构和验证要求。

## 9. 智训

### 9.1 功能介绍

智训是系统核心模块。系统根据业务规则、训练源文件和预期结果生成 Python 脚本，执行脚本后与预期结果进行比较，再依据差异和用户反馈持续修正，最终保存可用于正式计算的脚本版本。

### 9.2 训练输入与配置

| 项目 | 说明 |
| --- | --- |
| 租户 | 脚本、训练记录和结果的隔离边界 |
| 源文件 | 支持多个 `.xlsx`、`.xls`、`.xlsm` 文件 |
| 目标文件 | 预期结果或模板；不同生成模式下含义不同 |
| 规则文件 | 支持 `.md`、`.txt`、`.pdf`、`.docx` 和 Excel 文件，可多选 |
| 薪资年月 | 可选，格式建议为 `YYYY-MM` |
| 标准工时 | 可选，供薪资公式或规则使用 |
| AI 模型 | 从管理员启用的模型中选择 |
| 生成模式 | 公式模式、自动模式、模板填充模式、直接导入 |
| 多 Sheet 读取 | 开启后读取源文件全部 Sheet，并结合 Sheet 名和表头结构匹配 |
| 使用历史数据 | 开启后训练会接入历史月度结果；智算时会校验前序月份数据是否齐全 |
| 手动表头 | 复杂表头时通过 JSON 指定文件、Sheet 和表头起止行 |

手动表头示例：

```json
{
  "员工数据.xlsx": {
    "Sheet1": [3, 4]
  }
}
```

### 9.3 四种生成模式

| 模式 | 设计目标 | 适用场景 | 关键特点 |
| --- | --- | --- | --- |
| 公式模式 | 在结果 Excel 中写入可追溯公式 | 薪资计算、需要复核公式的场景 | 结果透明，便于业务检查公式 |
| 自动模式 | 由 AI 自由设计数据处理逻辑和输出 | 规则复杂、目标结构明确但不要求保留模板格式 | 灵活度高，依赖充分验证 |
| 模板填充模式 | 在目标模板中写值或公式 | 必须保留固定版式、格式和模板结构 | 需要目标模板；可选择需要处理的目标 Sheet |
| 直接导入 | 将源文件直接作为输出 | 无需 AI 计算，只需纳入统一脚本/计算流程 | 不调用 AI 生成业务逻辑，完成后准确率视为 100% |

### 9.4 标准使用说明

1. 选择租户，点击“新建”；
2. 打开“文件与配置”，上传训练素材并设置参数；
3. 对模板/自动模式，根据提示选择需要处理的目标 Sheet；
4. 保存配置并开始训练；
5. 观察实时状态、AI 输出、代码生成和执行日志；
6. 查看本轮准确率、字段差异样例、匹配单元格数和差异文件；
7. 在输入框中描述问题原因或修改方案：
   - “发送”用于分析、讨论和澄清，不立即执行代码；
   - “执行修正”根据讨论结果修改代码并重新验证；
   - “重新生成”使用当前附件中的新文件从头生成，未重新上传的素材继续沿用；
8. 达到可接受结果后点击“设为最佳”；
9. 下载当前代码、试算结果、差异对比、原始规则、提示词或最终整理规则。

### 9.5 会话与素材管理

- 左侧按租户显示训练会话历史；
- 可打开历史会话并查看消息、迭代、代码和下载项；
- 支持会话重命名和删除；
- 已有会话可以更新文件与配置；
- 源文件更新支持“新增/同名替换”和“替换全部源文件”；
- 正在执行的轮次不会被中途替换，保存的新配置在当前轮次结束后生效；
- 训练原始文件、规则、提示词、结果、差异文件和代码均可按会话追溯。

### 9.6 人工代码与最佳脚本

- 可上传 `.py` 代码并立即用会话中的完整源数据进行验证；
- 模板模式脚本可同时上传依赖的 Excel 模板；
- 上传的代码在通过安全检查后才执行；
- “设为最佳”会将当前代码保存为新的脚本版本，并记录实测准确率；
- 最佳脚本并不要求必须达到 100%，是否可投入生产应由业务验收标准决定。

### 9.7 训练输出

- 当前/最佳 Python 脚本；
- 每轮试算结果 Excel；
- 与预期结果的差异对比 Excel；
- 每轮提示词、AI 原始响应、生成代码、执行摘要和错误详情；
- 训练准确率、总迭代次数和耗时；
- 最终规则 Markdown；
- 版本化脚本记录。

### 9.8 状态与异常处理

训练会话主要状态为 `running`、`completed`、`failed`、`cancelled`；训练轮次主要状态为 `running`、`completed`、`failed`。

系统会处理或提示以下常见异常：

- 源文件、目标文件或规则缺失；
- 文件被密码保护；
- 复杂表头识别错误；
- 目标 Sheet 选择不正确；
- 生成代码不完整或未通过安全检查；
- 脚本超时、内存超限或执行失败；
- 输出文件未生成、生成多个候选结果或结果结构异常；
- 会话原始文件被清理，导致无法继续迭代；
- 并发任务排队或同一训练资源冲突。

## 10. 智算

### 10.1 功能介绍

智算使用智训沉淀的脚本处理新一期源数据。用户选择租户和脚本版本，上传正式数据后，系统进行结构检查、脚本执行、公式计算、结果选择和结果下载。

### 10.2 使用说明

1. 选择或输入租户 ID；
2. 从“计算版本”中选择启用的脚本；
3. 上传一个或多个源 Excel；
4. 如果脚本需要模板，可上传新模板；未上传时使用训练时模板；
5. 可填写薪资年月和标准工时；
6. 点击“开始计算”；
7. 查看状态、进度条和实时执行日志；
8. 计算完成后下载结果文件；
9. 如需正式报表、PDF、分组文件或历史周期报表，到“智能小工具—计算历史”中继续生成。

### 10.3 关键规则

- 普通用户只能操作已授权租户或自己训练过的租户；
- 计算脚本必须处于启用状态；
- 文件结构会根据训练时保存的源结构、表头和配置进行检查；
- 开启历史数据的脚本会检查所需前序月份计算结果；
- 任务日志以流式方式展示，断线时可通过状态接口恢复最终状态；
- 计算结果、任务耗时、错误、所用脚本、薪资年月和输出摘要会写入历史记录。

### 10.4 计算任务状态

新版任务模型支持以下阶段：

`pending` → `analyzing` → `analyzed` → `computing` → `completed`，异常时进入 `failed`。

其中分析阶段可生成表头映射和分析报告，执行阶段使用用户确认后的映射运行脚本。

## 11. 智能小工具

### 11.1 Sheet 拆分

#### 功能介绍

系统识别每个 Sheet 内的多个数据区域，取区域上方的 banner 作为分组依据，将一个 Sheet 拆成多个子 Sheet，命名格式为“原 Sheet 名-banner”。单区域或没有 banner 的 Sheet 保持原样。

#### 使用说明

1. 上传一个或多个 Excel；
2. 支持点击选择或拖拽上传；
3. 点击“开始拆分并下载”；
4. 系统处理每个文件并返回 ZIP。

#### 输入输出

- 输入：`.xlsx`、`.xls`、`.xlsm`，支持多选；
- 输出：包含拆分后 Excel 的 ZIP 压缩包。

### 11.2 多表数据合并

#### 功能介绍

把多个 Excel 按主键横向归并成一张结果表。用户可选择展示字段，将同名或同义列合并为一个结果列，并识别同一主键下的值冲突。

#### 使用说明

1. 上传至少两个 Excel；
2. 点击“解析字段”；
3. 选择合并模式和基准文件；
4. 为每个文件选择单主键或复合主键；
5. 选择需要输出的字段并调整最终列顺序；
6. 可点击“匹配同义列”：
   - 不使用 AI 时只合并同名列；
   - 使用 AI 时补充识别名称不同但语义相同的列；
7. 人工检查并修改输出列名及来源列；
8. 点击“生成合并文件并下载”；
9. 如需复用，可保存为命名方案；如需固定格式，可先生成模板骨架并编辑后上传到方案。

#### 合并模式

| 模式 | 输出范围 |
| --- | --- |
| 全量并集 | 输出所有文件中出现的主键 |
| 以基准文件为准 | 只输出基准文件中的主键 |
| 仅冲突 | 只输出存在多源值冲突的主键 |

#### 主键处理

- 可把 `1.0` 与 `1` 归一为同一主键；
- 日期键可选择关闭、按年月日、按年月、按月或按日归一；
- 同一主键在多表映射到同一结果列但值不一致时，结果会复制为多行并整组标红；
- 单一来源字段直接拼接，不参与多源冲突标红。

#### 方案与缓存

- 字段匹配按“列头指纹”缓存；文件名或数据变化但结构不变时可免 AI 复用；
- 列结构变化后指纹变化，缓存自动失效；
- 保存的方案包括主键、模式、归一化设置、输出字段、来源映射、列顺序和可选模板文件；
- 带模板的方案保留模板格式和公式，不带模板时输出纯数据。

### 11.3 多表整合对比

#### 功能介绍

该功能以一个 Excel 为主表，把一个或多个对照表中的值按关联键回填到主表指定列，并可生成差异 Sheet。系统保留主表整个工作簿、其他 Sheet、未修改列及其公式，只更新主表激活页的指定列。

#### 使用说明

1. 新增方案，或在方案列表中选择“应用/修改”；
2. 上传至少两个 Excel，选择一个主表和至少一个对照表；
3. 解析字段并检查每个文件的表头范围；识别错误时可手工指定起止行重新解析；
4. 为每个文件指定关联键；
5. 配置覆盖字段：选择主表目标列及对照表来源列；
6. 可配置跨列公式，或使用精确/AI 智能匹配；
7. 可配置对比字段；
8. 选择输出方式：只更新主表，或主表加差异 Sheet；
9. 生成并下载，结果会展示命中行数、覆盖单元格数和差异行数；
10. 配置可保存、修改、另存为、导出 JSON 或从 JSON 导入。

#### 公式能力

覆盖字段支持多列组合和受控公式，包括：

- 四则运算和比较运算；
- `ROUND`、`IF`、`ABS`、`MIN`、`MAX`、`SUM`、`AND`、`OR`、`NOT`；
- 文本拼接；
- 跨对照表引用；
- 同一主键在对照表出现多行时，数值列先跨行汇总再参与计算，文本取首个非空值。

#### 方案复用规则

- 方案按列头指纹识别本次上传文件的角色；
- 同结构文件导致角色歧义时，由用户手工确认“方案角色—本次文件”的对应关系；
- 方案列名和本次列名不一致时，系统给出建议映射，用户确认后仅对本次执行生效；
- 应用方案前会校验必需列和关联键，缺列时阻断执行并给出具体原因；
- 同组织及子组织可见方案；只有创建人或管理员可覆盖修改原方案，其他用户只能另存为。

### 11.4 模板管理与报表生成

#### 功能介绍

系统模板用于把智算结果加工成固定格式的业务报表。模板可为全局模板，也可绑定具体租户。

#### 模板配置

| 配置 | 说明 |
| --- | --- |
| 名称、描述 | 模板识别信息 |
| 作用域 | 全局或租户级 |
| 文件名规则 | 控制输出文件名称，可引用字段或字段切片 |
| 加密方式 | 不加密、打开密码或写保护 |
| 报表模式 | 普通填充、分块、ZIP 分组、按 Sheet 分组 |
| 分组字段 | 分块/ZIP/Sheet 模式的分组依据 |
| 块间空行 | 分块模式各分组之间的间隔 |
| 命名字段 | ZIP 内文件命名使用的字段 |
| 文件级拆分字段 | 例如按部门拆成多个文件 |
| 空周期显示 | 多月合并时是否显示没有数据的月份 |
| 整表搬运 | 将计算结果中的指定 Sheet 原样追加到报表末尾 |

#### 使用说明

1. 管理员在“模板管理”中新建或维护模板；
2. 上传模板 Excel，并配置作用域、输出模式、命名和加密规则；
3. 用户在“计算历史”选择已完成任务，点击“下载报表”；
4. 选择模板；如需多期合并，开启历史数据并选择薪资周期范围；
5. 系统按模板标记填充数据、计算公式并生成 Excel、PDF 或 ZIP 等结果。

#### 关键说明

- 模板字段既可使用 Excel 列字母，也可使用解析后的原始表头别名；
- 系统尽量保留模板自身的数字格式、公式和样式；
- 模板删除采用停用方式，避免影响历史记录；
- 全局模板对有权限用户可见，租户模板受租户授权范围限制。

### 11.5 训练历史

提供按租户模糊查询的训练记录，展示训练时间、模式、状态、准确率、迭代数等信息，并可查看或下载相关脚本及文件。该功能用于复盘训练过程、定位版本来源和审计结果。

### 11.6 计算历史

提供按租户、年月和状态筛选的计算任务列表，展示脚本版本、任务状态、耗时和结果。已完成任务可以下载原始计算结果，或基于模板生成正式报表。

### 11.7 数据对比

#### 功能介绍

对两个结果 Excel 进行单元格级比较，计算总体匹配率、差异单元格数量和各 Sheet 匹配情况，并生成差异报告。

#### 使用说明

1. 上传待比较的两个 Excel；
2. 填写一个或多个主键列，例如“工号,中文姓名”；
3. 点击“开始对比”；
4. 查看总体和各 Sheet 的匹配情况；
5. 下载差异对比 Excel；
6. 可在对比历史中重新下载已有报告。

主键缺失、Sheet 缺失或行数差异会影响匹配率；系统会在存在结构风险时给出警告。

### 11.8 SOP 维护

#### 功能介绍

SOP 维护用于管理“客户 + SOP 描述”的工作条目。每次上传源文件、结果文件和可选规则文件后，系统先按后台规则进行 AI 评估；AI 通过后进入人工审核。被打回的 SOP 可以修订后进入下一轮，所有轮次均保留。

#### 使用说明

1. 点击“新建 SOP”，填写客户名称和 SOP 描述；
2. 上传一个或多个源文件、一个结果文件和可选规则文件；
3. 系统选择客户专属规则；没有专属规则时使用全局规则；
4. AI 输出是否通过、评分、总结、问题和建议；
5. AI 通过后，复核员选择“审核通过”或“打回重写”；
6. 被打回后，上传人根据审核意见完善材料并再次上传；
7. 在详情中查看每一轮文件、AI 评价、人工结论、审核人和时间。

#### 状态

- SOP 条目：`draft`、`ai_analyzing`、`ai_failed`、`ai_passed`、`completed`、`rejected`；
- 单轮记录：`ai_analyzing`、`ai_passed`、`ai_failed`、`completed`、`rejected`、`failed`。

### 11.9 智能组表

#### 功能介绍

智能组表根据源 Excel、目标模板和可选规则自动识别字段关系、生成代码并填充模板。系统通过代码存档、字段映射知识库和人工反馈降低重复 AI 调用，并持续提高同类任务的稳定性。

#### 使用说明

1. 选择租户；也可留空并使用通用工具租户；
2. 选择全局或租户专属组表规则；不选规则时由 AI 仅按结构分析；
3. 选择 AI 模型；
4. 上传一个或多个源 Excel 和一个模板 Excel；
5. 如需忽略历史代码和知识库，勾选“强制重新匹配”；
6. 点击“开始组表”，查看分析、生成、执行进度和 AI 生成代码；
7. 下载原版结果或纯值版结果；
8. 对结果反馈“正确”或“有误”；
9. 有误时逐列核对“目标表头—源表—源列”映射，可修改后重新组表；
10. 确认正确映射后，系统更新字段知识库。

#### 缓存与知识闭环

- 任务签名由规则、源结构和模板结构共同决定；完全命中时可直接复用代码存档；
- 同名字段属于精确匹配，可直接采用；
- AI 语义匹配先进入候选状态；
- 语义映射被人工确认累计达到 2 次后进入可自动采用状态；
- 结果标错时不批量停用所有映射，而是让用户逐列复核并只修正相关字段；
- 强制重新匹配会跳过代码存档和知识库，执行完整 AI 分析。

#### 状态

`pending` → `analyzing` → `generating` → `executing` → `completed`；异常时进入 `error`。

## 12. 系统管理

### 12.1 用户管理

- 新建、查看、编辑和删除用户；
- 设置显示名、邮箱、手机、所属组织、角色和启用状态；
- 重置密码或直接设置新密码；
- 停用后的用户无法通过认证。

### 12.2 角色管理

- 新建、修改和删除角色；
- 配置角色名称、描述和权限集合；
- 系统角色受保护，不能按普通自定义角色方式随意删除；
- 权限支持菜单级和操作级控制。

### 12.3 组织管理

- 新建、修改和删除组织；
- 支持父子组织层级；
- 普通用户的租户可见范围可扩展到其所属组织的子组织；
- 多表整合方案也按组织树共享。

### 12.4 租户授权

- 将租户授权给组织；
- 授权类型包括 `owner` 和 `shared`；
- 支持撤销授权；
- 同一租户可以按业务需要授权给多个组织；
- 普通用户的可操作租户为“已授权租户 + 自己训练过的租户”。

### 12.5 基础数据管理

#### 功能介绍

基础数据用于维护最低工资、薪资日历或其他计算所需参考数据。资产可为全局级或租户级，并支持分类、版本和生效区间。

#### 主要能力

- 管理基础数据分类；
- 单文件或批量上传；
- 异步解析并显示上传进度；
- 查看文件大小、Sheet 摘要、解析表头和解析数据；
- 下载、预览、修改元数据、停用或删除；
- 上传新版本；
- 配置生效日期、失效日期、描述和自由标签；
- 筛选全局/租户级和已停用资产。

### 12.6 脚本管理

- 按租户筛选脚本；
- 查看脚本名称、模式、版本、准确率、状态、来源训练会话和更新时间；
- 启用、停用或删除脚本；
- 支持租户范围内的脚本状态管理；
- 停用脚本不会继续出现在正常智算版本中，但历史任务仍保留来源关系。

### 12.7 测试环境脚本迁移

- 从配置的测试环境拉取可迁移脚本；
- 按名称、来源租户、代码哈希和模式筛选；
- 批量选择并迁入目标租户；
- 可在迁入时指定新名称；
- 通过代码哈希和迁移记录识别已迁移脚本，降低重复导入风险。

### 12.8 SOP 规则管理

- 上传全局 SOP 大规则；
- 上传客户专属规则；
- 同一作用域只保持一条当前有效规则；
- 支持启停、下载和删除；
- 客户专属规则优先于全局规则。

### 12.9 智能组表规则与映射知识库

- 上传全局组表规则或租户专属规则；
- 支持规则重新上传、下载和删除；
- 查询字段映射知识库，可按源列、目标列和状态筛选；
- 查看模板指纹、匹配类型、确认次数和状态；
- 可编辑或删除错误映射；
- 映射状态包括候选、正常和待复核。

### 12.10 系统配置

管理员可统一启用或停用 AI 提供者。规则整理和智训页面只显示已经启用的模型，便于统一控制可用能力、成本和合规范围。

## 13. 辅助与兼容能力

除当前主要页面外，系统还保留以下接口级能力，用于兼容旧流程或支持实施运维：

- Excel 加密检测与密码处理；
- 旧版普通训练和流式训练；
- 旧版普通计算、流式计算和任务状态查询；
- 按字段拆分计算结果；
- 计算结果重新校验；
- 对比历史及差异文件下载；
- 下载租户脚本、训练日志、训练详情和历史导出；
- 基于反馈调整代码；
- 邮箱账户配置与邮件检查，可用于接收业务附件的集成场景；
- 数据资产化的两阶段计算接口：先分析表头映射，再确认执行。

这些能力主要服务于兼容、调试或集成，不一定在当前主导航中提供独立入口。新功能优先复用对话式智训、任务化智算和统一历史能力。

## 14. 权限与数据隔离设计

### 14.1 权限层次

1. 身份认证：确认用户是否登录且处于启用状态；
2. 菜单权限：决定首页、规则、智训、智算、工具和管理菜单是否显示；
3. 功能权限：控制拆表、合并、整合、SOP、智能组表及其创建、审核、管理动作；
4. 管理权限：控制用户、角色、组织、租户授权、基础数据、脚本和系统配置；
5. 数据权限：根据租户授权、组织树、创建人和任务归属过滤数据。

### 14.2 租户隔离

- 训练文件、脚本、计算结果和组表结果按租户目录隔离；
- 数据库对象记录 `tenant_id`，接口访问时校验用户可见租户；
- 全局资产和全局模板对符合权限的用户共享；
- 租户级资产、模板、规则和任务只在相应授权范围内可见；
- 工具的通用临时租户数据按用户归属进一步限制，避免不同用户互相查看。

### 14.3 组织继承

普通用户可以访问其所属组织及子组织被授权的租户。整合方案同样支持本组织和子组织可见，但编辑原方案仍受创建人或管理员限制。

## 15. 核心数据对象

| 对象 | 用途 | 关键字段 |
| --- | --- | --- |
| Role | 角色和权限集合 | 名称、权限、系统角色标记 |
| Organization | 企业组织树 | 名称、上级组织、状态 |
| User | 登录用户 | 用户名、组织、角色、状态 |
| TenantAuthorization | 租户与组织授权关系 | 租户、组织、授权类型、撤销时间 |
| Template | 正式报表模板 | 作用域、文件、模式、分组、命名、加密 |
| DataAsset | 基础/源/结果数据资产 | 类型、分类、版本、生效期、解析数据 |
| TrainingSession | 智训会话 | 租户、模式、配置、状态、最佳准确率 |
| TrainingMessage | 智训对话 | 角色、消息类型、内容、轮次元数据 |
| TrainingIteration | 单轮训练 | 提示词、AI 响应、代码、执行结果、准确率 |
| Script | 可复用计算脚本 | 租户、代码、模式、版本、准确率、状态 |
| ComputeTask | 正式计算任务 | 脚本、状态、映射、日志、结果、年月 |
| RuleSession | 规则整理会话 | 附件名、消息、最终规则 |
| MergeTemplate | 多表合并方案 | 主键、模式、字段映射、模板文件 |
| IntegrateTemplate | 多表整合方案 | 文件角色、指纹、覆盖/对比配置、组织归属 |
| AssembleTask | 智能组表任务 | 签名、代码存档、映射、反馈、输出 |
| AssembleFieldMapping | 组表字段知识 | 源列、目标列、模板指纹、确认次数、状态 |
| SopEntry / SopRound | SOP 条目与轮次 | 客户、材料、AI 评价、人工审核、状态 |

## 16. 技术架构设计

### 16.1 技术栈

| 层次 | 技术 |
| --- | --- |
| Web 后端 | FastAPI / Python 3.11 |
| 前端 | 服务端 HTML + 原生 JavaScript/CSS |
| 数据库 | SQLAlchemy ORM；生产可用 PostgreSQL，本地可用 SQLite |
| Excel 引擎 | Aspose.Cells for .NET，通过 pythonnet 调用 |
| AI 接入 | 统一提供者抽象，支持 OpenAI、Claude、DeepSeek、Ollama 等 |
| 文件存储 | 按租户目录隔离的本地文件存储 |
| 实时进度 | SSE/ReadableStream + 任务日志缓冲区 |
| 脚本执行 | 受控执行环境与独立子进程 |

### 16.2 模块分层

- `frontend/templates`：首页、规则、智训、智算、工具和管理页面；
- `frontend/static`：页面交互、鉴权、进度流和样式；
- `backend/api`：资产、训练、计算、规则、工具、组表等业务接口；
- `backend/ai_engine`：规则整理、提示词、代码生成、训练迭代和 AI 提供者；
- `backend/utils`：Excel 解析、匹配、比较、写入、校验和资源控制；
- `backend/sandbox`：AI 生成代码的安全执行；
- `backend/database`：用户、权限、资产、会话、脚本和任务数据；
- `backend/storage`：租户文件和脚本版本存储。

### 16.3 长任务处理

训练、计算、资产解析和智能组表可能占用较多时间和内存，系统采用以下策略：

- 上传请求与后台处理解耦；
- 通过并发信号量限制高成本 Excel 任务；
- 在独立子进程中执行 Excel 解析、脚本和写文件，任务结束后由操作系统回收内存；
- 通过 SSE 推送状态、日志、代码片段和完成事件；
- SSE 中断时使用状态接口兜底；
- 对超时、内存超限和子进程异常退出进行明确标记；
- 日志异步写盘并按天轮转，减少高频流式日志阻塞业务线程。

## 17. API 分区

| 前缀 | 作用 |
| --- | --- |
| `/api/auth` | 登录、退出、当前用户、修改密码 |
| `/api/admin` | 用户、角色、组织、授权、模板、脚本、迁移、模型配置 |
| `/api/assets` | 数据资产分类、上传、任务、预览、下载、版本管理 |
| `/api/rules` | 规则整理会话查询、更新和删除 |
| `/api/training` | 训练会话、迭代和脚本查询 |
| `/api/training/chat` | 对话式智训、素材更新、修正、代码上传和下载 |
| `/api/compute2` | 两阶段计算任务：分析、确认执行、结果与历史 |
| `/api/dashboard` | 首页租户计算状态 |
| `/api/tools` | Sheet 拆分、合并、整合、SOP 及相关方案 |
| `/api/assemble` | 智能组表规则、任务、进度、结果和反馈 |
| `/api/train`、`/api/calculate` 等 | 旧版训练、计算、比较、下载和兼容接口 |

启动服务后可通过 FastAPI 的 `/docs` 查看实际接口参数和响应模型。

## 18. 文件与输出约定

### 18.1 文件格式

- 核心 Excel 流程主要支持 `.xlsx`、`.xls`，部分模块同时支持 `.xlsm`；
- 规则附件支持 Markdown、文本、PDF、Word 和 Excel；
- Python 人工代码只接受 `.py`；
- 整合方案导入导出使用 JSON；
- Sheet 拆分多文件结果使用 ZIP；
- 报表可按模板配置生成 Excel、PDF 或 ZIP。

### 18.2 Excel 解析约定

- 支持标题行、多行表头、合并单元格和多数据区域；
- 自动识别不足时允许手动指定表头起止行；
- 对需要参与比较的 ID、工号、银行卡号等字段应尽量保留文本类型，避免前导零或长数字精度丢失；
- 写入结果后根据需要重新计算公式；
- 对模板、公式和宏有强依赖的场景，应使用小样本先做完整验收。

## 19. 安全、审计与可靠性

### 19.1 安全设计

- JWT/Bearer 令牌认证；
- 角色、功能、组织和租户多层权限校验；
- 租户文件物理隔离；
- 上传文件名和下载路径进行安全处理；
- AI 生成或人工上传代码在执行前进行代码检查，并在受控环境/独立进程中运行；
- 对 Excel 密码保护进行识别和显式处理；
- 限制长任务并发、运行时间和内存。

### 19.2 审计与追溯

- 训练会话保留消息、提示词、AI 响应、代码、准确率和差异；
- 脚本记录来源会话、创建人、版本和启停状态；
- 计算任务记录使用脚本、年月、日志、结果和耗时；
- SOP 保留每一轮 AI 分析和人工审核；
- 方案记录创建人、修改人和时间；
- 应用日志按天轮转并保留历史周期。

### 19.3 业务验收原则

- “准确率 100%”表示在当前比较规则和训练样例下完全匹配，不等同于覆盖所有未来数据边界；
- 规则、源结构、目标模板或基础数据变化后必须重新验证；
- 上线前至少准备正常、空值、重复主键、异常值、跨月和边界日期样例；
- 对薪资、税务等高风险结果，应保留人工抽样和汇总勾稽；
- AI 匹配建议必须允许人工修正，低置信度映射不得直接视为业务事实。

## 20. 常见问题与处理建议

| 问题 | 建议处理 |
| --- | --- |
| 表头解析不正确 | 使用手动表头，明确文件、Sheet 和起止行 |
| 训练准确率长期不提升 | 下载差异报告，先讨论差异原因，再执行修正；必要时补充规则或重做目标样例 |
| AI 修正破坏原有逻辑 | 回看历史轮次代码，上传人工修正版并重新验证，确认后设为最佳 |
| 智算找不到脚本 | 检查租户是否正确、脚本是否已设为最佳并处于启用状态 |
| 正式数据无法匹配 | 检查文件名、Sheet、表头、数据类型和手动表头是否与训练一致 |
| 日期或数字主键对不上 | 开启主键归一化，并选择正确的日期粒度 |
| 多表合并结果出现红行 | 同一主键的多源字段值冲突，需检查来源数据或调整字段归组 |
| 整合方案无法应用 | 根据阻断提示补齐必需列，确认文件角色，或重新指定表头 |
| 智能组表映射错误 | 选择“有误”，逐列修正映射并重新组表，不要直接确认错误映射 |
| 任务长时间无响应 | 查看实时日志和状态；关注排队、超时、内存超限或子进程异常信息 |
| 文件有密码 | 按页面提示输入对应密码；确认密码正确且文件未损坏 |

## 21. 推荐上线与日常操作规范

### 21.1 新租户上线

1. 建立组织、用户和租户授权；
2. 上传并验证必要的全局/租户基础数据；
3. 整理规则并由业务负责人确认；
4. 使用脱敏但结构真实的样例智训；
5. 准备多组验证数据，不只依赖单月样例；
6. 设为最佳脚本并完成脚本启用审批；
7. 用正式小批量数据试算和人工复核；
8. 建立所需报表模板；
9. 正式启用并保留首月重点复核。

### 21.2 月度运行

1. 检查基础数据和历史数据是否齐全；
2. 从首页进入待计算租户；
3. 确认脚本版本和薪资年月；
4. 上传当月源文件并执行；
5. 检查日志、结果行数、关键汇总和异常提示；
6. 下载计算结果或生成正式报表；
7. 将异常反馈到智训会话，修正后形成新脚本版本；
8. 保留旧版本，确认新版本稳定后再停用旧脚本。

## 22. 功能验收清单

### 22.1 通用

- 登录、退出、令牌过期和停用用户行为正确；
- 菜单和按钮与角色权限一致；
- 普通用户无法查看未授权租户数据；
- 上传、预览、下载、异常提示和中文文件名正常。

### 22.2 规则与智训

- 多种附件可正确解析；
- 规则会话可保存、恢复和下载；
- 四种训练模式均按设计执行；
- 会话素材更新语义正确；
- 每轮代码、准确率、差异和文件可追溯；
- 上传代码可验证，最佳脚本可形成新版本。

### 22.3 智算与报表

- 可正确选择租户和脚本版本；
- 结构不匹配时给出明确错误；
- 结果文件、公式和日志正确；
- 历史任务可查询和下载；
- 报表模板的填充、分组、拆分、命名、加密和历史周期功能正确。

### 22.4 智能工具

- Sheet 多区域拆分正确；
- 多表合并主键、同义列、冲突标红和模板方案正确；
- 整合对比覆盖、公式、差异 Sheet、方案复用和权限正确；
- SOP 的 AI 分析、人工审核和多轮闭环正确；
- 智能组表的缓存、知识库、反馈、修正和重新组表正确。

## 23. 术语说明

| 术语 | 含义 |
| --- | --- |
| 租户 | 一套相互隔离的客户/业务数据和脚本空间 |
| 源文件 | 提供原始业务数据的 Excel |
| 目标文件/预期结果 | 训练时用于说明最终输出结构和值的样例 |
| 模板 | 用于保持固定格式、公式或版式的 Excel 文件 |
| 脚本 | 智训生成或人工上传、可供智算执行的 Python 程序 |
| 准确率 | 试算结果与预期结果在比较规则下的单元格匹配比例 |
| 手动表头 | 用户显式指定表头所在行，覆盖自动识别结果 |
| 列头指纹 | 根据字段结构生成的稳定标识，用于识别同结构文件和复用方案 |
| 最佳脚本 | 用户确认后保存、可供正式智算选择的脚本版本 |
| 纯值版 | 将公式计算结果固化为值的输出文件，便于交付和跨环境查看 |

---

本文档描述的是当前代码已经提供的功能和设计边界。若页面、权限、数据模型或接口后续发生变化，应同步更新本文档的实现基线和相关章节。
