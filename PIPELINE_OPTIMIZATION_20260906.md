# 智训、规则整理与智算优化记录（2026-09-06 至 2026-09-07）

## 本次结论

发现并修改了规则证据丢失、日期误读、主表选择不确定、关联行数膨胀、重复解析/保存以及子进程无限等待等问题。本次只修改代码和本地配置，未部署服务器、重启服务或批量覆盖已有训练脚本。工作区原有修改和 examples 文件保持原状。

## 规则文件究竟会给 AI 看什么

- 源/目标 Excel：多行样例、字段类型和格式、原始公式；独立扫描全表公式，保留不同分支、绝对引用及原坐标，相同填充逻辑去重。公式扫描不依赖缓存值，也不局限于前几行。支持 XLSX、XLSM，以及 Aspose 可读取的 XLS。
- 作为规则附件上传的 Excel：输出各 Sheet 的单元格坐标、内容、公式和合并区域。XLSX/XLSM 还保留文本批注及图片存在提示。
- Word SOP：按正文中段落、标题、表格的实际先后顺序提取；PDF 标明页次。
- 长 SOP：不再直接截掉尾部。全文按段提交给 AI，附带完整标题提纲及相邻段上下文，随后整合所有分段提取结果。`RULE_ORGANIZE_MAX_DOC_CHARS` 现在是单段处理阈值，不能理解成尾部丢弃上限。
- 证据优先级：用户明确覆盖要求优先；否则以示例 Excel 已明确的公式逻辑为准，SOP/其他规则文档补充没有公式定义的部分；冲突单独说明。

**限制**：图片、扫描件中的内容没有在本次新增 OCR/视觉识别，提取结果会提示缺失，不能说图中所有信息已经交给 AI。长文分段提取仍依赖模型质量；最终合并提示若超过所选模型上下文，会报错，不保证任意大小文档都能一次完成。加密文件需要有效密码；完整公式扫描失败时会注明范围限制。

## 正确性修改

| 环节 | 原问题 | 修改后的行为 |
|---|---|---|
| 日期 | 日期数字可能被当成 Unix 纳秒；YYYYMMDD 与序列号混淆 | 区分日期字符串、YYYYMMDD、Excel 序列号；保留空值与非法原值；支持 1900/1904 纪元 |
| 标识符 | 整数/浮点/文本混用，长数字可能损失精度 | 统一文本键时不经浮点转换；保留前导零；源表文本形式的 `=...` 不当作公式执行 |
| 主表/主键 | 模糊匹配拿第一张表，随意选唯一值列 | 优先明确声明，模糊匹配必须唯一；仅在标识符候选中评分，检查列确实存在 |
| 多表关联 | 重复右键使行数膨胀，空键互相匹配 | 支持复合键；空键不相互匹配；查找型关联保持左表顺序和行数；重复右键明确报错 |
| 清洗与关联顺序 | 先关联再清洗，无法先解决一对多 | 日期处理 → 业务清洗/汇总 → 合表 → 写入源 Sheet |
| 数据写入 | 全表多轮遍历和反复扫描列边界 | 预计算列策略，在一次单元格遍历中应用值、类型、格式 |
| 旧 XLS 编码修复 | 用 xlrd 纯值导出覆盖文件会丢失公式/格式 | 仅修复文本及 Sheet 名，保留原公式、数字和格式 |
| 输出后处理 | 主键归一可能用 PutValue 删除公式 | 公式单元格不改值；格式、Sheet 改名和最终计算在同一会话完成 |
| 成功判定 | 映射失败继续计算、缓存不完整也交付成功 | 缺失映射/公式缓存失败直接报错；默认公式错误和损坏引用也不发布为成功结果 |

查找型关联不是任意一对多展开引擎：需要按业务规则先汇总，或明确复合键。已写死在旧训练脚本中的 AI 逻辑不会自动全部迁移，相关脚本需要重新训练/生成。没有执行线上 AI 调用，不能保证模型生成的全部业务规则都已通过真实样本验算。

## 9 月 7 日追加链路检查

- 智训与智算统一执行入口参数，覆盖脚本内烘焙的旧年月默认值，支持关键字参数和标准工时别名。对比前走相同的结果后处理，随后复用已计算缓存。
- 样例工作簿在内存计算公式，避免无缓存样例被读成空值；不改写上传样例。对比识别表头复用已打开工作簿，打开样例或计算失败时也释放 native 工作簿。
- 删除历史满分跳过当前验证、以及用历史高分代码替换本次低分代码的路径。每轮公式训练使用独立临时输出目录，防止覆盖预期文件或拿到旧输出。
- 对比保留前导零、复合键各组成部分，区分空值与零；使用 Decimal 比较长数字和金额。多余结果行降低准确率，空对比、缺表及部分表读取失败不能得到有效满分。数值绝对容差由 `COMPARISON_NUMERIC_TOLERANCE` 控制，默认 `0.000000001`，一分钱差异仍会报告。
- 单结果表也按 Sheet 名称匹配，不使用源数据页或按 Sheet 顺序回退；含数字前缀的业务结果表参与校验。无法唯一定位来源月份、主键列或 Sheet 时明确报错；声明复合键时不能悄悄少用一列。
- 公式和模板脚本的回退加载统一公式计算、源格式处理、手动表头、多 Sheet 开关、XLSM 支持与 Sheet 键分配。某个文件解析失败时停止，禁止部分输入继续算。后台全量加载失败后不再让脚本重新解析同一大文件；等待期限由受控子进程统一负责。
- 智训、智算使用相同结果文件选择：有多个文件时可排除与模板字节相同的副本，剩余多个候选则报错，不按修改时间猜测。人工“设为最佳”仍保存用户选定代码，但保留本轮实测准确率，并记录人工选定标记；不能把该操作显示为实测 100%。

重复主键的对比仍按每个键内部出现顺序配对，不等价于业务层完成了去重；业务需要合并、汇总或联合主键时，应在规则中明确。直接导入模式的文件透传不属于公式训练验证。本次未重新训练全部历史脚本，也未逐一替换所有旧版维护接口。

## Excel 读写复用范围

正常未加密 XLSX 路径中，预检查将结构检测、源值读取、必要的源公式计算和日期格式修复放到同一次 Aspose 打开中；日期修复在内存进行，不改写上传源文件。

预检查解析结果写入任务目录内的缓存，API 进程只拿检查结果，避免把全量 DataFrame 再搬入 API 内存。正式智算核对源文件名、大小、修改时间以及结构/映射配置后复用缓存。配置或文件变化时重新解析。缓存不是跨任务共享，也不接收用户上传的 pickle。

预加载成功后，不再把源文件逐个改写到 mapped_source，再用训练加载器全部重读；键名和字段类型通过内存映射保持一致。

输出文件后处理复用一个工作簿，依次处理主键、目标/源格式、汇总样式、Sheet 改名，最后计算公式并保存至临时结果，再原子替换为最终结果。行数统计复用内存工作簿；纯值副本复用已验证缓存，不再重复计算。

**范围边界**：这是普通源文件一次解析、输出后处理一次打开/一次最终保存。旧脚本仍可能先生成初始工作簿；加密文件解密、旧 XLS 转换、覆盖模板改名、模板读取、结果归档和可选纯值副本有独立读写。不能声称整个系统从上传到下载只发生一次文件打开或磁盘写入。

## 排队、内存与强杀

原本地配置是单个智算进程 2048MB、重任务并发 3、常驻池 1；全量源文件解析内部还会并行打开最多 4 个工作簿。这个组合会叠加内存和磁盘压力。

已更新本地 `.env` 及 `.env.example` 中的资源项：

| 配置 | 当前值/含义 |
|---|---|
| `EXCEL_WORK_CONCURRENCY` / `SUBPROCESS_CONCURRENCY` | 1，重任务默认串行 |
| `EXCEL_PARSE_WORKERS` / 智训线程并发 | 1，避免单任务内并行展开多个工作簿 |
| `SUBPROCESS_POOL_SIZE` | 0，默认关闭常驻池，结束后回收进程内存；仍支持显式开启 |
| 各阶段 `*_MAX_MEMORY_MB` / `CODE_SANDBOX_MAX_MEMORY` | 0 表示自动预算，不再统一固定 2GB；正数表示额外上限 |
| `EXCEL_MEMORY_RESERVE_MB` | 最少 256MB；实际保留量取该值、256MB、有效总内存 15% 的最大值 |
| `EXCEL_TASK_MEMORY_CEILING_MB` | 8192MB，只是预算最高值；机器或容器可用内存不足时会降低 |
| `EXCEL_QUEUE_MAX_PENDING` | 32 个等待者；满队列拒绝新增等待 |
| `EXCEL_QUEUE_MAX_WAIT` | 1800 秒，共享执行槽等待上限 |
| `SUBPROCESS_QUEUE_TIMEOUT` | 600 秒，子进程执行槽/资源等待上限 |
| `SUBPROCESS_KILL_GRACE_SECONDS` | 5 秒；Windows taskkill 命令本身另有最多 5 秒期限 |
| `COMPUTE_STRICT_FORMULA_ERRORS` | true；业务若确实允许错误值，需要明确调整此策略 |

内存预算读取 Windows 可用物理内存或 Linux MemAvailable，同时考虑容器 cgroup 内存限制；运行中也会因剩余可用内存减少而收紧预算。机器只有 2GB 总内存时，不会给任务分配 8GB。单个工作簿本身超过安全预算仍会失败，不能靠取消上限使它无限运行。

子进程运行、输出管道排空以及强杀后退出等待都有期限。Linux 工作进程使用独立进程组；Windows 终止进程树，修正了回退 API 的 64 位句柄类型。工作池先确认退出再补位，旧 reader 不会把新 worker 标记为死亡。

强杀期限结束仍未退出时，当前服务进程会停止派发新 Excel 重任务，保留尚被占用的临时文件，不自动不断拉起替代进程。训练请求取消时，尚在运行的线程继续占有资源槽直至真实结束。上传分块写入移出事件循环，减少慢 I/O 阻塞所有请求的情况。

**部署范围**：闸门和故障状态是进程级的；当前 Dockerfile 使用 `--workers 1`。多 API worker、多实例部署需要外部统一队列/资源协调器，不能把本实现当分布式调度。内存监控主要读取任务进程 RSS，不能替代虚拟机和宿主存储监控。操作系统阻塞的磁盘调用不能被 Python 超时强制恢复。

Docker 内存限制现在可通过 `APP_MEM_LIMIT` 配置，默认仍为 8g，交换限制与之相同。本次没有调整服务器实际内存、磁盘驱动或虚拟机设置。

## 验证与性能证据

离线回归命令：

```powershell
python -m pytest backend/test_pipeline_accuracy.py backend/test_resource_and_output.py backend/test_training_compute_chain.py backend/test_code_generation_stream_fallback.py backend/test_template_migration_paths.py backend/test_compute_smart_rename.py backend/test_report_formula_recalculation.py backend/test_formula_recalculation.py -q
```

结果：98 项通过（2026-09-07，7.16 秒）。包括真实子进程成功/错误/超时/取消、模拟不可杀进程的停止派发、队列公平性与等待上限、源解析单次打开及源文件不变、1904 日期、模板格式恢复、跨 Sheet 改名后的公式缓存、XLS 公式保留、合表与生成脚本执行，以及聊天智训实际执行与对比、样例隔离、历史高分回退、多输出歧义、人工选定保持实测评分等链路回归。Python 编译检查、前端 JavaScript 语法检查及 Git diff 空白检查通过。未调用真实 AI 服务，未做生产服务器压测或故障注入。

写入微基准：`python tools/benchmark_source_writer.py --rows 3000 --columns 80 --repeats 3`。同机 24 万单元格，旧 HEAD 写入中位数 1.124 秒，新写入 0.630 秒，约 1.78 倍；逐格值、类型、格式和样式摘要一致。该数字仅代表源 Sheet 写入阶段，不代表整次智算提速比例。

## 历史会话文件与中途修改（9 月 7 日追加）

历史会话顶部现在固定展示当前源文件、目标文件、规则附件及下载链接，不再把文件入口放在容易滚出视野的聊天记录顶部。文件读写接口校验当前用户可操作租户。对于配置仍指向已删除临时目录或旧机器路径的会话，会尝试恢复该会话自身的持久化文件；找不到实际文件时提示重新上传，不从其他会话猜测文件。

“文件与配置”新增保存入口。已有会话默认新增源文件、同名替换，其他源文件保留；也可明确选择替换全部源文件。目标文件可单独替换，规则附件支持 Word、PDF、文本和 Excel，AI 提供者、年月、标准工时、多 Sheet 开关及手动表头会恢复为会话已保存值，并可继续修改。发送消息、执行修正和重新生成前会先保存待提交的修改，避免页面改了但后台仍用旧配置。

上传先进入暂存目录，再通过共享队列进行验证；当前轮次结束后生效。文件更新使用独立版本目录，验证通过才切换配置，失败保留旧文件和旧配置。更新目标文件时重新解析公式与结构、刷新模板引用、清除旧目标 Sheet 选择；输入变化后旧评分及差异不再作为当前输入的验证结论。历史迭代和原版本文件保留。

新增 14 项文件管理回归，加上前述链路测试共 **112 项通过**。另有离线前端交互测试：`node tools/test_training_assets_ui.cjs`，覆盖配置恢复、合并上传、AI 切换、下载链接绑定正确会话和保存失败保留待上传文件。真实 Excel 验证了目标替换、模板更新、公式证据及同名规则附件替换。Python/JavaScript 语法及 Git diff 检查通过；未使用真实账号执行浏览器登录或真实 AI 调用。

## 部署与磁盘故障边界

## 启动数据库诊断（9 月 7 日追加）

本机启动报错定位为远程 PostgreSQL 端点瞬时不可达：首次探测 TCP 连接超时，同网段邻居状态为 `Incomplete`，随后网络恢复，数据库查询与完整 FastAPI 启动均成功。数据库连接现在默认在 5 秒内失败，并在堆栈末尾给出不含用户名和密码的中文诊断，区分连接超时、端口拒绝、认证失败和数据库不存在；连接池等待默认 10 秒。可通过 `DATABASE_CONNECT_TIMEOUT` 和 `DATABASE_POOL_TIMEOUT` 调整。

启动程序创建示例文件的行为改为“仅缺失时创建”，防止每次启动覆盖 `examples` 中已有工作簿。完整启动验证已确认应用完成数据库检查并监听 18000 端口，随后正常关闭测试进程。

此前本机日志出现 E: 的 NTFS 140 / I/O 总线重置记录，但仅凭这些信息不能确定根因。这次减少并发、重复文件操作和强杀后的任务堆积，不能证明已经修复宿主磁盘、控制器或虚拟磁盘层的问题。

代码与本地配置需要在服务重启后生效；服务器需要同步相应代码及资源配置，不能只更新 `.env.example`。旧生成脚本需重新训练才能获得新骨架逻辑。建议首次上线以真实样例中已明确公式计算出的结果逐项核对，并观察单任务峰值内存与队列等待；这一步本次尚未在生产执行。
